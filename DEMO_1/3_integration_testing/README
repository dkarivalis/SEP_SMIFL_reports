ALL TESTS ARE LOCATED WITH THE APPLICATION IN ../1_code/team1_demo1.tgz

To Run Integration Tests, set up the project following the given tutorials. 
Then once project directory is set up, proceed to the SEP_SMIFL directory.
In this directory run "sudo play test"

This will run both integration and unit tests.

The code for the tests is located under the "test" directory.
